"use strict";

const {
    encodeRoute,
    encodeCompositeMetadata
} = require("rsocket-core");

module.exports = {
    encodeRoute(route) {
        return encodeCompositeMetadata([
            [0x7E, encodeRoute(route)]
        ]);
    }
};